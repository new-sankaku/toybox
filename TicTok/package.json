{
  "name": "tictok-frontend-tests",
  "private": true,
  "version": "0.0.0",
  "description": "static/ の classic script に対する unit test 基盤 (vitest + jsdom)",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.4",
    "jsdom": "^26.1.0",
    "vitest": "^3.2.4"
  }
}
