# Server config (secret). Copy to .env and fill in. .env is gitignored.
# EulerStream sign server API key. Empty = anonymous tier (low rate limit).
# https://www.eulerstream.com/
TICTOK_EULER_API_KEY=
