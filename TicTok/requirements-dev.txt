-r requirements.txt

pytest>=8.0
pytest-asyncio>=1.0
httpx>=0.27

# 静的解析。設定は pyproject.toml、運用手順は doc/LINT_TYPECHECK.md。
# ruffはminor版で新ruleが既定に入りうるため上限を固定する(CIが自分の変更と無関係に
# 赤くなるのを防ぐ)。更新は意図的に行い、その版でbaselineを取り直す。
ruff>=0.16,<0.17
mypy>=2.3,<3.0
