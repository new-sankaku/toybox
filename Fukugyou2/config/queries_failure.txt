# 失敗事例の検索語（人が書いた語）。
# 既定の経路では使いません。検索語は tools/discover_queries.py が data から作ります。
# この file は `run_pipeline.py --no-discover` で自動発見と比較するときの対照用です。

"shutting down"
startup died
"Ask HN" failed startup
"failed startup"
"we failed"
"post-mortem" startup
"shut down" my saas
"we're shutting down"
startup failure lessons
"why we failed"
"we are shutting down"
"lessons learned" failed startup
saas failed customers
