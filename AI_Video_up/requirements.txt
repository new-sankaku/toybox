# AI_Video_up の Python 環境
#   0_環境を作る.bat から使う。Windows 11 / Python 3.10 / CUDA 12.6 / RTX 4070 Ti で組んだ版。
#
#   torch と torchvision は PyPI に cu126 版が無いため、index を足して入れる:
#     pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cu126

# --- 本体(vup.py / vfi.py が要る) ---
torch==2.10.0+cu126
torchvision==0.25.0+cu126
numpy==1.26.4
opencv-python==4.10.0.84
pillow==10.2.0
psutil==5.9.4

# 高画質化 model の読み込み
spandrel==0.4.2
spandrel_extra_arches==0.2.0

# TensorRT 経路(--no-trt なら無くても動く)
tensorrt==11.2.1.2
onnx==1.21.0
onnxruntime-gpu==1.22.0
onnx-graphsurgeon==0.6.1
polygraphy==0.53.4
onnxconverter-common==1.16.0

# torch.compile(--no-trt の時の既定経路)
triton-windows==3.7.1.post27

# GPU 常駐 pipeline と GPU 使用率の計測
pynvvideocodec==2.2.0
cupy-cuda12x==13.6.0
nvidia-ml-py==13.610.43

# --- 計測 script だけが要る(exp_* / bench_* / d* / m* / r* / s* / t*) ---
scipy==1.15.3
lpips==0.1.4
scikit-image==0.25.2
imageio==2.37.0
omegaconf==2.3.0
nvidia-modelopt==0.46.0
